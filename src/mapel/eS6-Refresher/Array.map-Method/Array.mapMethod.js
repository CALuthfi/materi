import React, { Component } from "react";

export default class ArraymapMethod extends Component {
  render() {
    const colors = ["red", "green", "blue"];
    const item = colors.map(color => `<li>${color}</li>`);

    console.log(items);
    return (
      <div>
        <p>lo</p>
      </div>
    );
  }
}
